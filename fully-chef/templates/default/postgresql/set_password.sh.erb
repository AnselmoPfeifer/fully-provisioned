#!/usr/bin/env bash

sudo -i << EOF
    set -x
    su <%= node['postgresql']['default_username'] %>
    cd /var/lib/postgresql/
    psql -c "ALTER ROLE postgres WITH PASSWORD '<%= node['postgresql']['default_password'] %>';"
    exit
EOF